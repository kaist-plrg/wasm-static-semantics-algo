;;
;; General Variable Conventions
;;

syntax N = nat
syntax M = nat
syntax n = nat  ;; hack
syntax m = int  ;; hack
syntax i = nat
syntax j = nat

dec $min_(nat, nat) : nat
def $min_(i, j) = i  -- if $(i <= j)
def $min_(i, j) = j  -- otherwise